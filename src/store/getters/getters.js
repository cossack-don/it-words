const getters = {

    GETTERS_GET_ARRAY_WORDS(state) {
        return state.arrayItemsWords
    },
    GETTERS_SEARCH_INPUT(state) {
        return state.flagMessageNoWord
    }
}

export default getters